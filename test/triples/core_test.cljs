(ns triples.core-test
  (:require [clojure.test :refer [deftest testing is]]
            [triples.core :as core]
            [triples.deck :as deck]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
